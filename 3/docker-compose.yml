# docker-compose.yml
version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.production
    image: pletneval/devops-for-programmers-project-74:latest
    ports:
      - '8080:8080'

